/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'source.unsplash.com'], // Correctly combine the domains into one array
  },
};

export default nextConfig;
